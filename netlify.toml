[build]
  command = "npm run build"
  publish = "dist"

# /join → StreamYard (must come before any catch-all if you add one later)
[[redirects]]
  from = "/join"
  to = "https://streamyard.com/yifxpdcaf9"
  status = 302
  force = true

