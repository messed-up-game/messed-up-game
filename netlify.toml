[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Keep this allow rule
[[redirects]]
  from = "/.well-known/*"
  to   = "/.well-known/:splat"
  status = 200

# TEMPORARILY DISABLE the SPA catch-all for this deploy:
# [[redirects]]
#   from = "/*"
#   to   = "/index.html"
#   status = 200


