[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Pretty /join link → StreamYard (put specific routes BEFORE the catch-all)
[[redirects]]
  from = "/join"
  to = "https://streamyard.com/yifxpdcaf9"
  status = 302
  force = true

# Single Page App catch-all (MUST be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200


