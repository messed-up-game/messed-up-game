# show the workspace root VS Code opened
pwd

# list what's here
ls -la

# search a few levels deep for package.json (ignores node_modules)
find . -maxdepth 4 -name package.json -not -path "*/node_modules/*"


